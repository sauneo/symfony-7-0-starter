{% extends 'admin/base.html.twig' %}

{% block body_content %}

    <div class="card">

        <div class="card-body">

            <div class="table-responsive">

                <table class="table table-striped table-hover table-nowrap align-middle mb-0">

                    <thead>
                        <tr>
                            <th scope="col" class="text-center">{{ 'label.id'|trans({}, 'project')|lower }}</th>
                            <th scope="col">{{ 'label.pi_name'|trans({}, 'project')|lower }}</th>
                            <th scope="col" class="text-center">{{ 'label.actions'|trans|lower }}</th>
                        </tr>
                    </thead>

                    <tbody>
                    {% for project_contact in project_contacts %}
                        <tr>
                            <td class="text-center">{{ project_contact.id }}</td>
                            <td>{{ project_contact.piName }}</td>
                            <td class="text-center">
                                <a href="{{ path('app_admin_project_contact_show', {'id': project_contact.id}) }}" title="{{ 'action.show'|trans({}, 'project')|lower }}"><i class="ri-eye-fill"></i></a>
                                <a href="{{ path('app_admin_project_contact_edit', {'id': project_contact.id}) }}" title="{{ 'action.edit'|trans({}, 'project')|lower }}"><i class="ri-edit-fill"></i></a>
                            </td>
                        </tr>
                    {% else %}
                        <tr>
                            <td colspan="7">{{ 'message.no_records_found'|trans }}</td>
                        </tr>
                    {% endfor %}
                    </tbody>

                </table>

            </div>

        </div>
        
    </div>

    <a href="{{ path('app_admin_project_contact_new') }}" title="{{ 'action.create_new_project_contact'|trans({}, 'project') }}" class="btn btn-success">{{ 'action.create_new_project_contact'|trans({}, 'project') }}</a>
    
{% endblock %}

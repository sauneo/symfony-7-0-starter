{% extends 'admin/base.html.twig' %}

{% block body_content %}

    <div class="row mb-4">

        <div class="col-lg-12">

            {{ form_start(form) }}

                {{ include('admin/project_text/_form.html.twig') }}

                <div class="float-start">
                    <button class="btn btn-success">{{ 'action.update'|trans }}</button>
                    <a href="{{ path('app_admin_project_text_index') }}" title="{{ 'action.back_to_list'|trans }}" class="btn btn-secondary">{{ 'action.back_to_list'|trans }}</a>
                </div>

            {{ form_end(form) }}

            {{ include('admin/project_text/_delete_form.html.twig') }}
    
        </div>

    </div>

{% endblock %}

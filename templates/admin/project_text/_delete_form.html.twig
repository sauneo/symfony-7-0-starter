<form method="post" action="{{ path('app_admin_project_text_delete', {'id': project_text.id}) }}" onsubmit="return confirm('{{ 'alert.are_you_sure_you_want_to_delete_this_item'|trans }}');" class="d-inline-block float-end">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ project_text.id) }}">
    <button class="btn btn-danger">{{ 'action.delete'|trans }}</button>
</form>

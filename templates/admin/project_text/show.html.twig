{% extends 'admin/base.html.twig' %}

{% block body_content %}

    <div class="card">

        <div class="card-body">

            <div class="table-responsive">

                <table class="table table-striped table-hover table-nowrap align-middle mb-0">

                    <tbody>
                        <tr>
                            <th>{{ 'label.id'|trans({}, 'project')|lower }}</th>
                            <td>{{ project_text.id }}</td>
                        </tr>
                        <tr>
                            <th>{{ 'label.name'|trans({}, 'project')|lower }}</th>
                            <td>{{ project_text.name }}</td>
                        </tr>
                        <tr>
                            <th>{{ 'label.description'|trans({}, 'project')|lower }}</th>
                            <td>{{ project_text.description }}</td>
                        </tr>
                    </tbody>

                </table>

            </div>

        </div>

    </div>

    <div class="d-flex justify-content-between">
        <div>
            <a href="{{ path('app_admin_project_text_edit', {'id': project_text.id}) }}" title="{{ 'action.edit_project_text'|trans({}, 'project') }}" class="btn btn-success">{{ 'action.edit_project_text'|trans({}, 'project') }}</a>
            <a href="{{ path('app_admin_project_text_index') }}" title="{{ 'action.back_to_list'|trans }}" class="btn btn-info">{{ 'action.back_to_list'|trans }}</a>
        </div>
        <div>
            {{ include('admin/project_text/_delete_form.html.twig') }}
        </div>
    </div>

{% endblock %}

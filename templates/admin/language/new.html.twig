{% extends 'admin/base.html.twig' %}

{% block body_content %}

<div class="row mb-4">

    <div class="col-lg-12">

        {{ form_start(form) }}

        {{ include('admin/language/_form.html.twig') }}

        <div class="float-start">
            <button class="btn btn-success">{{ 'action.save'|trans }}</button>
            <a href="{{ path('app_admin_language_index') }}" title="{{ 'action.back_to_list'|trans }}" class="btn btn-secondary">{{ 'action.back_to_list'|trans }}</a>
        </div>

        {{ form_end(form) }}

    </div>

</div>

{% endblock %}
